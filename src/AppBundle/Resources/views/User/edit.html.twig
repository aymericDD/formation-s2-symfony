{% extends '::base.html.twig' %}

{% block title %}User edit{% endblock %}

{% block body -%}
    <div class="container">
        {{ form(edit_form) }}
        <hr>
        <a href="{{ path('user') }}" class="btn-default btn">
            Back to the list
        </a>
        <hr>
        {{ form(delete_form) }}
    </div>
{% endblock %}
