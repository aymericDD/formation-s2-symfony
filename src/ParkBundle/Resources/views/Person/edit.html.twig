{% extends '::base.html.twig' %}

{% block title %}Person edit{% endblock %}

{% block body -%}
    <div class="container">
        {{ form(edit_form) }}

        <ul class="record_actions list-unstyled">
            <li>
                <a class="btn btn-danger" href="{{ path('person') }}">
                    <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span> Back to the list
                </a>
            </li>
        </ul>
        <hr>
        {{ form(delete_form) }}
    </div>
{% endblock %}
