{% extends '::base.html.twig' %}

{% block title %}Ip edit{% endblock %}

{% block body -%}
    <div class="container">
        {{ form(edit_form) }}
        <hr>
        <a class="btn btn-danger" href="{{ path('ip') }}">
            <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span> Back to the list
        </a>
        <hr>
        {{ form(delete_form) }}
    </div>
{% endblock %}
